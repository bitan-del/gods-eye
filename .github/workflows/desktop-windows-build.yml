name: Desktop Windows Build

on:
  workflow_dispatch:
    inputs:
      arch:
        description: "Target architecture"
        required: false
        default: "x64"
        type: choice
        options:
          - x64
          - arm64

concurrency:
  group: desktop-windows-build
  cancel-in-progress: true

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"
  NODE_VERSION: "24.x"

jobs:
  build-windows:
    runs-on: windows-latest
    permissions:
      contents: write
    defaults:
      run:
        working-directory: apps/desktop
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}

      - name: Setup pnpm
        working-directory: .
        run: |
          corepack enable
          corepack prepare pnpm@10.23.0 --activate

      - name: Install root dependencies
        working-directory: .
        run: pnpm install --no-frozen-lockfile

      - name: Install desktop dependencies
        run: pnpm install --no-frozen-lockfile

      - name: Build with electron-vite
        run: npx electron-vite build

      - name: Bundle MCP servers
        run: node scripts/build-mcp-servers.js

      - name: Package Windows installer
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CI: "true"
        run: npx electron-builder --win --${{ inputs.arch || 'x64' }} --publish=never
        continue-on-error: false

      - name: List build output
        run: |
          echo "=== out/ ==="
          Get-ChildItem -Path out -Recurse -File -ErrorAction SilentlyContinue | Where-Object { $_.Extension -match '\.(exe|zip|msi)$' } | ForEach-Object { $_.FullName }
          echo "No matching files in out/" -ErrorAction SilentlyContinue
        shell: pwsh

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: gods-eye-windows-${{ inputs.arch || 'x64' }}
          path: |
            apps/desktop/out/**/*.exe
            apps/desktop/out/**/*.zip
          retention-days: 30
          if-no-files-found: warn
