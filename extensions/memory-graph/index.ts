/**
 * Memory Graph extension — MCP-only plugin (no TypeScript entry).
 * The actual MCP server runs as a standalone Node.js process defined in
 * `godseye.plugin.json` under `mcpServers`.
 */
export {};
