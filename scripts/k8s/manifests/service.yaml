apiVersion: v1
kind: Service
metadata:
  name: godseye
  labels:
    app: godseye
spec:
  type: ClusterIP
  selector:
    app: godseye
  ports:
    - name: gateway
      port: 18789
      targetPort: 18789
      protocol: TCP
