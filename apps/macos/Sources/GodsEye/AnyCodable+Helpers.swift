import Foundation
import GodsEyeKit

// Prefer the GodsEyeKit wrapper to keep gateway request payloads consistent.
typealias AnyCodable = GodsEyeKit.AnyCodable
typealias InstanceIdentity = GodsEyeKit.InstanceIdentity
